import React from 'react';
import './Hero.css';

const Hero = () => (
  <section className="hero">
    <h2>Celebrate with Spark!</h2>
    <p>Premium quality crackers at unbeatable prices</p>
    {/* <button className="btn-view">Shop Now</button> */}
  </section>
);

export default Hero;
